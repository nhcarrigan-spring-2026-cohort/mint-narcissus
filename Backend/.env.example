NODE_ENV="development"

MONGODB_URI="mongodb://admin:admin@localhost:27017/mint-narcissus"

JWT_SECRET="jwt-secret"
JWT_EXPIRE="1d"

# Service ports (for local dev; Docker Compose sets these in containers)
# gateway=8080, auth-service=3001, items-service=3002, requests-service=3003
